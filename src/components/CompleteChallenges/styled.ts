import styled from 'styled-components';

export const CompleteChallengesContainer = styled.div`
  height: auto;
`;

export const CompleteChallengestTextWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 3.4rem 0;
  padding-bottom: 1rem;
  border-bottom: 1px solid var(--gray-border);
  font-weight: 500;
`;

export const CompleteChallengesDefaultText = styled.p`
  font-size: 1.25rem;
`;

export const CompleteChallengesValue = styled.p`
  font-size: 1.5rem;
`;